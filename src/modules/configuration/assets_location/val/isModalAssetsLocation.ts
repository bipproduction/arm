"use client"

import { atom } from "jotai"

export const isModalAssetsLocation = atom(false)
export const isModalEditAssetsLocation = atom(false)
export const isModalCreateAssetsLocation = atom(false)