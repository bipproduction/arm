"use client"

import { atom } from "jotai"

export const isModalWarehouseLocation = atom(false)
export const isModalEditWarehouseLocation = atom(false)
export const isModalCreateWarehouseLocation = atom(false)